<!DOCTYPE html>
<html lang="pt-BR">
    <%- include('../../partials/head') %>
<body>
    <%- include('../../partials/header') %>

    <main class="container">
        <section class="main-column">
            <article class="post">
                <h2 class="title">Usuários</h2>
                <a href="/usuarios/cadastrar-usuario" class="btn">Cadastrar Usuário</a>

                <%- include('../../partials/msg') %>
                
                <table class="usuarios">
                    <thead>
                        <tr>
                            <th>Matrícula</th>
                            <th>Nome</th>
                            <th>Aposentado</th>
                            <th>Data de Cadastro</th>
                            <th>Atualizar</th>
                            <th>Mais detalhes</th>
                        </tr>
                    </thead>

                    <tbody>
                        <% usuarios.forEach(function(usuario){ %>
                            <tr>
                                <td><%= usuario.matricula %></td>
                                <td><%= usuario.nome %></td>
                                <td><%= usuario.aposentado %></td>
                                <td><%= usuario.data_cadastro %></td>
                                <td class="td-anchor"><a href="/usuarios/atualizar" class="btn fa fa-edit table-anchor"></a></td>
                                <td class="td-anchor"><a href="/usuarios/consultar/<%= usuario.id %>" class="btn fa fa-angle-double-right table-anchor"></a></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </article>
        </section>

        <%- include('../../partials/sidecolumn') %>
    </main>

    <%- include('../../partials/footer') %>
</body>
</html>